fragment ProductsGetListItem on Product {
	... on VariableProduct {
		id
		name
		variations(first: 50) {
			nodes {
				id
				name
				sku
				price(format: FORMATTED)
				regularPrice(format: FORMATTED)
				attributes {
					nodes {
						name
						label
						value
					}
				}
			}
		}
	}
}
