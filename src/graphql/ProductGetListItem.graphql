fragment ProductsGetListItem on Product {
	... on SimpleProduct {
		id
		slug
		name
		price
		image {
			...ProductGetMediaItem
		}
		attributes {
			nodes {
				id
				name
				options
			}
		}
	}
}
